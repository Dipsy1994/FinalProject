@page
@model FinalProject.Pages.Seats.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Seat[0].SeatNumber)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Seat[0].Class)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Seat[0].Availability)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Seat[0].Flight)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Seat) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.SeatNumber)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Class)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Availability)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Flight.FlightID)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.SeatID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.SeatID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.SeatID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
