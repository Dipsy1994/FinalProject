@page
@model FinalProject.Pages.Airports.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Airport[0].Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Airport[0].Code)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Airport[0].City)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Airport) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Code)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.City.CityID)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.AirportID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.AirportID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.AirportID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
